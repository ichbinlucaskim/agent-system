# Architecture - NN <Archetype name>

> Copy this file with the folder. Everything below is guidance to delete.

## Shape

<An ASCII diagram of the components and the direction data moves between them.
Draw only components you can source. A box you are guessing at is a box that
belongs in open-questions.md instead.>

```text
<diagram>
```

## Flow

<Numbered walkthrough of one request from arrival to result. Tier-tag each step
that asserts something about the real system. Steps that are our reconstruction
carry [T3] and say so.>

## Boundaries

<Where control passes between components, and what crosses each boundary. This
is usually where the interesting design decisions are, and it is usually the
part that outside reconstructions get wrong.>

## What this diagram does not show

<Every diagram is a claim about what matters. Name what has been left out and
whether it was left out because it is unimportant or because it is unknown.
The second kind belongs in open-questions.md as well.>
