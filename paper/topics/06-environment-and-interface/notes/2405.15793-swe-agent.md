# SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering

<https://arxiv.org/abs/2405.15793>

Yang, Jimenez, Wettig, Lieret, Yao, Narasimhan, Press. Princeton
University, Stanford University. NeurIPS 2024.

## Core ideas

The thesis is one sentence: **the interface an agent acts through is a
design variable, and designing it for the model rather than reusing the
one built for humans is worth more than changing the model.** The paper
names the artifact — an **agent-computer interface**, an abstraction
layer between the agent and the computer — by explicit analogy to
human-computer interaction, where nobody would expect a person to be
productive without a good editor.

The starting observation is concrete. An agent given a Linux shell
struggles: it cannot reliably edit a small segment of a file, and when it
makes an invalid edit **nothing tells it so**. The shell is highly
configurable and extremely granular, which suits an expert human and
suits a model badly.

The replacement is deliberately small: a handful of commands for
searching, navigating, viewing, and editing, sitting on top of the shell
rather than replacing it. Four design decisions carry the result.

- **Suppress verbosity.** Search commands return a summary and cap the
  number of results, because a wall of matches spends context without
  informing.
- **Guardrails on edits.** A linter runs inside the edit command.
  Invalid edits are **discarded** and the agent is asked to try again,
  with the relevant error and a snippet around it. The bad state never
  exists.
- **Specific, concise feedback every turn.** Including for the empty
  case: when a command produces no output, the interface says so
  explicitly rather than returning nothing, because nothing is
  ambiguous.
- **Context management.** Malformed generations trigger a retry, and
  **all past error messages except the first are dropped**. Observations
  older than the last few are collapsed to one line each. The interface
  actively curates what remains in the window.

The ablation is the evidence that this is not packaging: the same model
with the designed interface solves substantially more than the same model
with the plain shell. And the interface **ports to a different model**,
which is what makes it an interface rather than a prompt tuned to one
system.

## What problem it solves

Agent work had focused on the model, the prompt, and the scaffold, while
the action surface was taken as given — usually a shell, because it
exists. That inherits an interface optimized for a different kind of
user, one who can see the screen, undo, and read a manual.

The paper's contribution is to make that surface the object of study, and
to show the return on designing it is large and available **without
touching the model's weights**.

## Limitations

- **The interface was developed against one model** and then shown to
  port. Portability was demonstrated, not guaranteed, and an ACI can be
  overfitted to a model's habits.
- **Designed by hand, per domain.** There is no method for deriving a
  good interface, only demonstrated principles, so every new environment
  is a fresh design exercise.
- Scoped to software engineering, and evaluated on benchmarks in that
  domain.
- Building on the shell keeps its escape hatches available, so the small
  action space is a default rather than a boundary.

## What this means in practice

1. **Design the interface for the model, not for the person.** This is
   the whole paper and it generalizes past code. Exposing an existing
   admin API, CLI, or database directly to an agent is the same mistake
   as handing it a shell: those surfaces assume a user who can see
   results, undo mistakes, and consult documentation.

2. **Make the invalid state unrepresentable, and say why.** The linter
   inside the editor is the single most copyable mechanism here. Validate
   inside the action, reject before anything is written, and return the
   specific error with enough surrounding context to fix it. Compare with
   letting the write succeed and hoping a later step notices.

3. **Silence is ambiguous, so never return it.** Saying the command
   succeeded and produced no output is a tiny change that removes a real
   failure mode, where the agent cannot tell success from a broken tool
   and retries or invents. Every tool should have a defined message for
   the empty result.

4. **Cap and summarize what a tool returns.** Verbose output is not
   generosity; it is context spent on material the agent did not ask for,
   crowding out what it needs. A tool that can return a thousand matches
   should return the top few and say how many there were.

5. **Curate the history as part of the interface.** Dropping all but the
   first repeated error, and collapsing old observations to one line, are
   interface decisions rather than model decisions. This is the same
   discipline as 2310.08560's tiering, applied at the tool layer, and it
   is where much of the practical benefit lives.

6. **Measure interface changes the way you would measure model
   changes.** The ablation is what turns this from taste into
   engineering. Hold the model and the task fixed, change one aspect of
   the interface, and count. Tool wording, result formatting, and error
   text all move the number, and none of it is visible without the
   experiment.

7. **Portability is the test of whether you built an interface or a
   prompt.** If your careful tool design collapses when the model
   changes, you tuned to a quirk. Trying it on a second model is a cheap
   check with a clear meaning.

## What it leads to next

Nothing in this library extends it; within `06-environment-and-interface`
this is the most recent.

Backwards it answers **2310.06770** directly, and makes that benchmark's
own point: the task was left open to any method, and what moved the
number was the interface rather than the model. It extends
**2302.04761** in the sense the topic cares about — the question moves
from whether a model can call a tool to what the tool should look like —
and it takes the loop shape from **2210.03629**, generating a thought and
a command each step.

Read against **2305.16291**, the mirror image: that paper grows the
agent's vocabulary inside a fixed environment, this one redesigns the
primitives the agent is given.

## Connection to the lab

`papers.yaml` maps this to two labs, one for the interface itself and one
for shipping it.

**`labs/track-2-tools-context/07-tool-design-aci` is this paper as an
exercise**, down to the name. The lab's framing is the same claim: the
agent-computer interface is to an agent what a user interface is to a
person, and teams routinely spend days on prompt wording and minutes on a
tool description even though the description decides whether the tool is
called at all.

Three of the lab's rules are this paper's mechanisms. **The model sees
only the name, the description, and the schema** — everything it needs
must be in those three fields. **Schemas should make the wrong call hard
to express**, which is the guardrail principle moved from the editor into
the type. And **error messages are prompts**: what you return after a
failure is the next thing the model reads, so naming the problem and
listing valid inputs is what lets the agent fix its own call.

The lab's `compare_descriptions` is point six made into homework: hold
the questions fixed, vary only the tool surface, and report the selection
rate. That is this paper's ablation at a scale you can run yourself, and
it is what converts interface design from taste into a number.

**`labs/track-4-production/18-packaging-deploy` is the other half**, and
the connection is less obvious but real. This paper's system is an agent
plus an environment it depends on, and that dependency is exactly what
packaging has to carry. The lab's rules apply directly: **configuration
validated at startup rather than at first use**, because a process that
starts happily and fails an hour later is much harder to diagnose; and
**health checks that do not touch the model**, since a probe that makes
an API call reports the provider's availability rather than yours.

The lab's warning that a smoke test is not an evaluation is worth pairing
with this paper's ablation. One asks whether the thing starts and answers;
the other asks whether the interface is any good. A green pipeline and a
bad ACI coexist comfortably.

Elsewhere in the sibling repository:

- **Lab 10, an MCP-style server.** Where a tool surface becomes a
  protocol other systems consume, and the design decisions here become
  the contract.
- **Lab 11, context and memory.** Point five, and the observation that
  history curation belongs at the interface layer as much as at the
  agent layer.
- **Lab 15, evaluation.** Where the ablation lives once it stops being a
  one-off experiment.
- **Lab 12, the agent loop.** The thought-and-command step this system
  runs, and the budgets around it.
