"""Layer 5 — config, CLI/HTTP adapters, smoke, MCP stdio server.

Import ``packaging.config`` or ``packaging.app`` directly. This package
``__init__`` stays empty to avoid import cycles with ``agent.loop``.
"""
