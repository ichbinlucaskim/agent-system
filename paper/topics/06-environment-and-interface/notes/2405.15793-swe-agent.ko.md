# SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering

<https://arxiv.org/abs/2405.15793>

Yang, Jimenez, Wettig, Lieret, Yao, Narasimhan, Press. Princeton
University, Stanford University. NeurIPS 2024.

## 핵심 아이디어

주장은 한 문장입니다. **agent가 행동하는 인터페이스는 설계 변수이고, 사람을 위해
만들어진 것을 재사용하는 대신 모델을 위해 설계하는 것이 모델을 바꾸는 것보다
값어치가 크다.** 논문은 그 산출물에 이름을 붙입니다. **agent-computer
interface**, 즉 agent와 컴퓨터 사이의 추상 층이고, 좋은 편집기 없이 사람이
생산적이기를 아무도 기대하지 않는 human-computer interaction에 대한 명시적 유비로
그렇게 합니다.

출발점의 관찰이 구체적입니다. Linux shell을 받은 agent는 헤맵니다. 파일의 작은
구간을 안정적으로 편집하지 못하고, 유효하지 않은 편집을 했을 때 **아무것도 그것을
알려 주지 않습니다.** shell은 매우 설정 가능하고 매우 세밀한데, 그것이 숙련된
사람에게는 맞고 모델에게는 나쁘게 맞습니다.

대체물은 일부러 작습니다. 검색과 이동과 보기와 편집을 위한 명령 한 줌이고, shell을
대체하는 것이 아니라 그 위에 얹힙니다. 설계 결정 넷이 결과를 떠받칩니다.

- **장황함을 억누릅니다.** 검색 명령이 요약을 돌려주고 결과 수에 상한을 겁니다.
  일치 항목의 벽은 알려 주지 않으면서 context를 씁니다.
- **편집에 대한 guardrail.** linter가 edit 명령 안에서 돕니다. 유효하지 않은 편집은
  **버려지고** agent에게 다시 해 보라고 요구하며, 해당 오류와 그 주변 조각을 함께
  보여 줍니다. 나쁜 상태는 아예 존재하지 않습니다.
- **매 턴 구체적이고 간결한 feedback.** 빈 경우까지 포함해서입니다. 명령이 출력을
  내지 않으면 인터페이스가 아무것도 돌려주지 않는 대신 그렇다고 명시합니다. 아무것도
  없는 것은 모호하기 때문입니다.
- **Context 관리.** 형식이 깨진 생성은 재시도를 부르고, **첫 번째를 뺀 과거 오류
  메시지는 전부 버립니다.** 최근 몇 개보다 오래된 observation은 한 줄로 접힙니다.
  인터페이스가 창에 남는 것을 적극적으로 큐레이션합니다.

이것이 포장이 아니라는 증거가 ablation입니다. 같은 모델이 설계된 인터페이스로
평범한 shell보다 상당히 많이 풉니다. 그리고 그 인터페이스가 **다른 모델로
옮겨집니다.** 그것이 이것을 한 시스템에 맞춰 조정된 prompt가 아니라 인터페이스로
만드는 것입니다.

## 무엇을 해결하는가

agent 연구는 모델과 prompt와 scaffold에 집중했고, action 표면은 주어진 것으로
받아들였습니다. 대개 shell이었는데, 그것이 이미 있기 때문입니다. 그것은 다른 종류의
사용자, 즉 화면을 보고 되돌리고 매뉴얼을 읽을 수 있는 사용자에게 최적화된
인터페이스를 물려받는 일입니다.

논문의 기여는 그 표면을 연구 대상으로 만들고, 그것을 설계했을 때의 수익이 크며
**모델 weight를 건드리지 않고** 얻을 수 있음을 보인 것입니다.

## 한계

- **인터페이스가 한 모델을 상대로 개발된 뒤** 옮겨진다는 것이 보여졌습니다.
  이식성은 시연됐지 보장되지 않았고, ACI가 모델의 습관에 과적합될 수 있습니다.
- **영역마다 손으로 설계합니다.** 좋은 인터페이스를 도출하는 방법은 없고 시연된
  원칙만 있으므로, 새 environment마다 새 설계 작업입니다.
- 소프트웨어 공학에 한정되고, 그 영역의 benchmark로 평가됐습니다.
- shell 위에 짓기 때문에 그쪽의 탈출구가 계속 열려 있고, 작은 action space는 경계가
  아니라 기본값입니다.

## 실무에서 의미하는 것

1. **사람이 아니라 모델을 위해 인터페이스를 설계하세요.** 논문의 전부이고 코드
   너머로 일반화됩니다. 기존 admin API나 CLI나 데이터베이스를 agent에게 그대로
   노출하는 것은 shell을 쥐여 주는 것과 같은 실수입니다. 그 표면들은 결과를 보고
   실수를 되돌리고 문서를 찾아볼 수 있는 사용자를 가정합니다.

2. **유효하지 않은 상태를 표현 불가능하게 만들고, 이유를 말하세요.** 편집기 안의
   linter가 여기서 가장 베끼기 좋은 장치입니다. action 안에서 검증하고, 무엇이 쓰이기
   전에 거절하고, 고칠 수 있을 만큼의 주변 맥락과 함께 구체적인 오류를 돌려주세요.
   쓰기를 성공시키고 나중 단계가 알아채기를 바라는 것과 견줘 보세요.

3. **침묵은 모호하므로 절대 돌려주지 마세요.** 명령이 성공했고 출력이 없었다고
   말하는 것은 작은 변경이면서 진짜 실패 양식 하나를 없앱니다. agent가 성공과 고장난
   tool을 구분하지 못해 재시도하거나 지어내는 실패요. 모든 tool에는 빈 결과에 대해
   정해진 메시지가 있어야 합니다.

4. **tool이 돌려주는 것에 상한을 걸고 요약하세요.** 장황한 출력은 너그러움이 아니라
   agent가 청하지 않은 자료에 쓰인 context이고, 필요한 것을 밀어냅니다. 천 개의
   일치를 돌려줄 수 있는 tool은 상위 몇 개를 돌려주고 몇 개였는지를 말해야 합니다.

5. **이력 큐레이션을 인터페이스의 일부로 하세요.** 반복된 오류에서 첫 번째만 남기고
   버리는 것, 오래된 observation을 한 줄로 접는 것은 모델 결정이 아니라 인터페이스
   결정입니다. 2310.08560의 tiering과 같은 규율을 tool 층에 적용한 것이고, 실무적
   이득의 상당 부분이 여기 삽니다.

6. **인터페이스 변경을 모델 변경과 같은 방식으로 재세요.** ablation이 이것을 취향에서
   공학으로 바꾸는 것입니다. 모델과 task를 고정하고, 인터페이스의 한 측면만 바꾸고,
   세세요. tool 문구와 결과 서식과 오류 텍스트가 전부 숫자를 움직이고, 실험 없이는
   어느 것도 보이지 않습니다.

7. **이식성이 인터페이스를 지었는지 prompt를 지었는지의 시험입니다.** 공들인 tool
   설계가 모델이 바뀌자 무너진다면, 버릇에 맞춰 조정한 것입니다. 두 번째 모델에서
   해 보는 것이 값싸고 의미가 분명한 검사입니다.

## 다음으로 이어지는 것

이 라이브러리 안에서 이것을 확장하는 논문은 없습니다. `06-environment-and-interface`
안에서 가장 최근입니다.

거슬러 올라가면 **2310.06770**에 직접 답하면서 그 benchmark 자신의 주장을 증명합니다.
task는 어떤 방법에든 열려 있었고, 숫자를 움직인 것은 모델이 아니라 인터페이스였습니다.
이 topic이 신경 쓰는 의미에서 **2302.04761**을 확장합니다. 질문이 모델이 tool을 부를
수 있는가에서 tool이 어떻게 생겨야 하는가로 옮겨 갑니다. 그리고 매 단계 thought과
command를 생성하는 **2210.03629**의 루프 모양을 가져갑니다.

거울상인 **2305.16291**에 대어 읽으세요. 저쪽은 고정된 environment 안에서 agent의
어휘를 키우고, 이쪽은 agent에게 주어지는 원시 요소를 다시 설계합니다.

## lab과의 연결

`papers.yaml`은 이것을 lab 두 개에 연결합니다. 하나는 인터페이스 자체를 위한 것이고
하나는 그것을 내보내기 위한 것입니다.

**`labs/track-2-tools-context/07-tool-design-aci`가 이 논문을 연습 문제로 만든
것입니다.** 이름까지 그렇습니다. lab의 틀 잡기가 같은 주장입니다. agent-computer
interface는 agent에게 사용자 인터페이스가 사람에게 그런 것이며, 팀은 prompt 문구에
며칠을 쓰고 tool 설명에 몇 분을 쓰는데 그 설명이 tool이 불릴지 말지를 정합니다.

lab의 규칙 셋이 이 논문의 장치입니다. **모델은 이름과 설명과 schema만 봅니다** —
필요한 모든 것이 그 세 필드 안에 있어야 합니다. **schema는 잘못된 호출을 표현하기
어렵게 만들어야 하고**, 이는 guardrail 원칙을 편집기에서 타입으로 옮긴 것입니다.
그리고 **오류 메시지는 prompt입니다** — 실패 후에 돌려주는 것이 모델이 다음으로 읽는
것이므로, 문제를 이름 붙이고 유효한 입력을 나열하는 것이 agent가 자기 호출을 고칠 수
있게 하는 것입니다.

lab의 `compare_descriptions`가 6번을 숙제로 만든 것입니다. 질문을 고정하고 tool
표면만 바꾸고 선택률을 보고하기. 이 논문의 ablation을 당신이 직접 돌릴 수 있는
규모로 옮긴 것이고, 인터페이스 설계를 취향에서 숫자로 바꾸는 것이 그것입니다.

**`labs/track-4-production/18-packaging-deploy`가 나머지 절반이고**, 연결이 덜
분명하지만 실재합니다. 이 논문의 시스템은 agent에 그것이 의존하는 environment를 더한
것이고, 그 의존이 바로 packaging이 날라야 하는 것입니다. lab의 규칙이 그대로
적용됩니다. **설정은 첫 사용이 아니라 시작 시점에 검증되어야** 합니다. 잘 시작해
놓고 한 시간 뒤에 실패하는 프로세스가 진단하기 훨씬 어렵기 때문입니다. 그리고
**health check는 모델을 건드리면 안 됩니다.** API 호출을 하는 probe는 당신의 가용성이
아니라 제공자의 가용성을 보고하기 때문입니다.

smoke test는 평가가 아니라는 lab의 경고를 이 논문의 ablation과 짝지어 볼 값어치가
있습니다. 하나는 그것이 시작하고 답하는지 묻고, 다른 하나는 인터페이스가 쓸 만한지
묻습니다. 초록불 파이프라인과 나쁜 ACI는 사이좋게 공존합니다.

sibling repository의 다른 곳들:

- **Lab 10, MCP 방식 server.** tool 표면이 다른 시스템이 소비하는 프로토콜이 되는
  곳이고, 여기의 설계 결정이 계약이 되는 곳입니다.
- **Lab 11, context와 memory.** 5번, 그리고 이력 큐레이션이 agent 층만큼이나
  인터페이스 층에 속한다는 관찰.
- **Lab 15, evaluation.** ablation이 일회성 실험이기를 멈췄을 때 사는 곳입니다.
- **Lab 12, agent loop.** 이 시스템이 도는 thought-and-command 단계와 그 둘레의 예산.
